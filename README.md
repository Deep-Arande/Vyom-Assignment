# Vyom  React Assignment

A simple React application built using Vite that allows users to:

- 🔍 Search scans by **Scan ID**
- 🧑‍⚕️ Search scans by **Patient ID**
- 📅 Filter results using **Start Date** and **End Date**

The application consumes Dental Dost outbound APIs and displays the response in a clean UI.

---

##  Tech Stack

- React (Vite)
- React Router DOM
- Fetch API
- Environment Variables (.env)

---

##  Project Setup

### 1️⃣ Clone the Repository

```bash
git clone https://github.com/Deep-Arande/Vyom-Assignment.git
cd Vyom-Assignment

Install Dependencies
npm install



Create Environment File

Create a .env file in the root directory (same level as package.json) and add:

VITE_API_KEY=your_api_key_here


Run the Application
npm run dev